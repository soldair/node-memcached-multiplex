{
  "name": "memcached-multiplex",
  "description": "combine concurrent gets for the same keys into one get/multiget to the memcache server",
  "version": "0.0.0",
  "repository": {
    "url": "git://github.com/soldair/node-memcached-multiplex.git"
  },
  "main": "index.js",
  "scripts": {
    "test": "tape test/*.js"
  },
  "author": "Ryan Day",
  "devDependencies": {
    "tape": "~2.1.0",
    "memcache-server-stream": "~0.0.1",
    "memcached": "~0.2.6"
  }
}
